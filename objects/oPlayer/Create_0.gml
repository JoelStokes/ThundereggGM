/// @desc Set Player Pos
/*var newX = TileToScreenX(x, y);
var newY = TileToScreenY(x, y);

show_debug_message(newX);
show_debug_message(newY);

x = newX
y = newY*/

//What is causing TileToScreen to not work here? Where does player go?

xsp = 0;
ysp = 0;
depth = -100;